# Handoff — 2026-08-06

Continues [`2026-07-26-handoff-birds.md`](./2026-07-26-handoff-birds.md), which
covered `e73b768`…`f421b30`. This one covers `17e5838`…`dd182bd` — 14 commits,
~9,100 lines across 98 files. All pushed to main; Vercel auto-deploys.

Two things make this session different from the last two.

**The first is that I destroyed a child's data.** Not a bug I inherited — a
command I ran, twice, that deleted 1,623 of Cecily's correct answers and closed
every garden bed she had earned. It was recoverable and it is recovered, and the
seed can no longer do it. §2 is the whole story, and it is the part of this
document worth reading if you read nothing else.

**The second is that Esme finally got some attention.** Three of the fourteen
commits are hers, and they came out of reading her rows rather than guessing:
she had tried the bird module and bounced off it, she had finished the only Luna
chapter that existed, and a fifth of her sessions ended without a single answer.
All three turned out to be actionable. See §5.

---

## 1. What shipped

### Birds Phase 2 — the audio half
`17e5838` `902a17a` `df3d9e4`

The whole xeno-canto → ffmpeg → audition → upload pipeline, plus the Listen and
Match stages. 15 clips across all 10 original birds are live.

The pipeline is four npm scripts and the middle one matters:
`birds:audio-harvest` → `birds:audio-clips` (proposes a 6-second window by
band-passed loudest-window analysis, cuts a preview and a spectrogram) →
`birds:audition` (a local server on :4517 where a **human ear** confirms or
nudges every window) → `birds:audio-upload`. Nothing uploads without a
`selections.json` written by that audition. That constraint is not squeamishness:
nothing in this codebase can hear, the loudest six seconds of a field recording
can be a lawnmower, and the audition step is where a person decides which
cardinal sounds most like a cardinal.

Two judgement calls worth keeping. Clip exercises pin to the **first voice of a
kind**, because a clip reference is `{birdCode, kind}` and the Blue Jay's two
calls have opposite pitch shapes — an exercise built from the second would
contradict the clip that actually plays. And there is deliberately **no
"how many repeats" exercise**, even though the catalog carries `repeats`: that
field is the typical phrase count, any individual recording may differ, and an
exercise must never mark a child wrong for hearing the clip correctly.

### Birds Phase 3 — the world
`f4d6fcc`

`bird_feeder` is the eighth habitat, at the most open point on the garden map.
All ten birds are in `SPECIES_CATALOG` with codes matching the bird catalog, so
they arrive after sessions and live beside the feeder. **Tapping a bird plays its
call** (call before song — the call is what she would actually hear out of a
window most of the year). The **life list** records birds she has really seen,
sorted rarest-first, granting the noticing gem on a first sighting only.

The ten birds share one parameterised drawing rather than ten separate ones —
what differs between them is exactly what the curriculum teaches her to look at:
cap, crest, bib, cheek, eyebrow, wing bars, bill weight, tail length.

`93f393b` then made a sighting feel like an event: her photograph back at full
width, the bird's voice playing itself, a fact she was **not** taught (teach
pages lead with `facts[0]`, so a sighting hands back the next one), and the
arrival chime the garden already uses.

### Cliffside Point, and division as a real thing
`d96f346`

Division had **no strand**. All five `math.divide.*` skills lived inside
`multiplication`, which is why it never surfaced as a destination in the compass,
the planner, or Hodge's recommendations. It now has its own strand — that is the
change that separates it everywhere — and its own station on the right flank of
Math Mountain, holding four skills including `mm4_leftover_rocks` moved out of
High Meadow. Long division stays on the Summit; it is Level 5.

Stop positions came from a solver searching the free flank for maximum clearance
(38 units at worst against every tap circle, label pill and station box), not
from placing them by eye. That is the direct lesson of Mirror Tarns.

**The plan's premise had gone stale.** It described Cecily as "barely doing
division"; by the time I built it she had *mastered* `divide.facts_to_10` and
`divide.unknown_factor` with two more in review. The structural fix was still
right — division genuinely had no place of its own — but it opened already
half-complete. Worth re-checking a plan's facts before building from it.

### The Winter Visitors
`dd182bd`

Crew 3: Dark-eyed Junco, White-throated Sparrow, American Tree Sparrow, Purple
Finch, Cedar Waxwing. Five birds that **are not here in summer**. In August the
units do not exist; around mid-October they appear; in April they go.

Units carry `availableMonths`, **not** the flora `Season` enum, and the
difference is the entire feature: juncos reach Louisville in mid-October, which
meteorological season-splitting calls autumn. Gating on `currentSeason() ===
'winter'` would hide the crew until December, six weeks after she has been
watching them under the feeder. Out of season a unit is **absent, not
padlocked** — a padlock promises "practise and this opens", and no amount of
practice brings a junco to Kentucky in July.

Two of the five are the confusion pairs the design named, and both separate on
the calendar before any field mark: Purple Finch against the House Finch she
already knows, and American Tree Sparrow against the summer Chipping Sparrow.
The Cedar Waxwing earns its place differently — it eats berries and will never
come to a seed feeder, which is how a child learns a feeder is not the whole
world.

---

## 2. I deleted 1,623 of Cecily's correct answers

**The event.** `npm run db:seed`, run twice on the live database — once to
register the `bird_feeder` habitat, once after the division strand split —
deleted every `attempt` row pointing at a seed item. 1,623 correct answers for
Cecily, 338 for Esme. Garden beds unlock from lifetime-correct, so her seven open
beds collapsed to one and her 30 earned seeds to four. She opened the app to find
her gardens shut.

**Why it was possible.** `seed-math.ts` and `seed-reading.ts` deleted attempts
before re-inserting items. The script *documented* this, in a comment, and
offered `SEED_ADDITIVE=1` to avoid it. Destructive by default, safe by opt-in.

Safety that has to be remembered is not safety. I had read that same file earlier
the same day — for the strand block — and did not read as far as the item block.

**The fix** (`b4129a8`). Additive is now the default; replacing items requires
`SEED_WIPE_ITEMS=1` and says so out loud; and even that path sets
`attempt.item_id = null` rather than deleting, so a child's lifetime record can
never be destroyed by seeding again. `tests/world/seedSafety.test.ts` reads both
scripts and fails on a delete against `attempt`, on safety hidden behind an
opt-in, and on a silent replace. Same shape as `migrationSafety.test.ts`, which
exists because a migration once wrote learner state — **this is the second time a
content operation has damaged a learner's own data.**

**The recovery** (`b4129a8`, `56ccde2`), and the reason it was possible at all:
the `session` table survived, carrying both `items_correct` **and**
`skill_planned` per session. So the totals *and* the per-skill attribution were
rebuildable as evidence rather than estimate.

`npm run restore:attempts` — dry-run by default, then `--write`, then
`--attribute`, with `--undo`. Restored rows are stamped
`response.source = 'history_restore'` so the whole repair stays identifiable and
reversible. Cecily's per-skill counts came back across 63 skills; her sight words
read 369 and her subtraction 241 again.

**What could not be recovered:** which individual item she answered. Those rows
are gone and their items were replaced, so restored rows point at a
representative item of the right skill — truthful at the skill level, which is
what the map displays, approximate below it. And ~30 rows from adaptive focus
sessions stay deliberately unattributed, because those sessions record
`focus.math` rather than a skill and there is no honest answer.

**The lesson, stated plainly:** a content operation must never write or erase
learner state, and "the script warns you in a comment" is not a control.

---

## 3. Four bugs that only a browser could catch

None of these is a logic error. All four passed the entire suite.

| bug | why the tests could not see it |
|---|---|
| **The wrong bird played.** The mourning dove cooing when the question asked about the robin. | An `<audio>` element only reads its `<source>` children once, during resource selection. React reused the node between questions and swapped the `src`, which an already-loaded element ignores. The generator was innocent — 2,040 generated clip exercises across every unit and 60 seeds have zero prompt/clip mismatches, which I checked *before* touching anything. |
| **The spectrogram was cropped**, showing a sliver. | `object-fit: cover` in a fixed-height box. On a photo a crop loses scenery; on a spectrogram the vertical axis is FREQUENCY, so it deletes notes. Uncropped, the pictures turned out to be poor on their own terms too — a linear scale squashes a 500 Hz dove into the bottom pixels — so all 15 were redrawn on a log scale. |
| **A blocked microphone was a dead end.** | Both mic item types gated their fallback on whether speech was *supported*. With speech supported and permission *blocked*, the screen said "use the buttons below" and the button rendered only in the unsupported case. No way to answer, no exit but to leave. |
| **Esme's tier offered a bird she had never met.** | `otherBirds()` leads with `confusableWith`, which crosses crews on purpose. At Level 1 that meant choosing between a chickadee and a White-breasted Nuthatch from a crew she cannot see. |

Three of the four were found by *looking at the running app*; one was found by
asking why a number in the session data looked wrong. Zero were found by tests.
Every one now has a test, and for the first three I confirmed the test fails with
the fix reverted — a regression test that passes either way is worthless.

---

## 4. Photo curation, again, and a new trap

The winter crew needed 16 new photographs. The junco contact sheet alone held a
nest of chicks, four shots of a blurred bird behind a chain-link fence, a
hand-drawn cartoon on a whiteboard, and **two dead birds**. The sparrow sheets
held two leucistic all-white individuals; the finch sheet a rare yellow morph.
Each would teach a child to look for a bird that does not exist.

**Three red males were sitting in the Purple Finch FEMALE set** — same family as
the Female=10/Male=11 trap, caught the same way: by looking.

**The new trap is subspecies.** A species-level iNat search for Dark-eyed Junco
returns mostly *western* Oregon Juncos — dark hood, brown back — while Kentucky
winters the Slate-colored form, which is uniform grey. Teaching the wrong one is
the same error as the Black-capped Chickadee's mnemonic. The catalog now has
`inatPhotoTaxonId`, an explicit per-bird override for harvesting a regional
subspecies. The catalog still names and teaches the species; only the photographs
are narrowed.

---

## 5. Esme

Everything here came from reading her rows. She is Level 1, has played 99
sessions since April, is still playing, and answers **90% of what she is given
correctly**. She is not struggling; she had run out of things aimed at her.

**She tried the birds and bounced.** `bird_review` carried a `crew1_look` entry
while `bird_units` stayed empty — she attempted the first unit and did not pass.
`42a7cc1` adds a **simple tier**: crew 1 only, always two choices, colour-and-name
only, no listening discrimination, six questions instead of nine. Tier comes from
learner level with the boundary deliberately between the two sisters, and a test
asserts the full tier's output is byte-identical so none of it touches Cecily.

**She finished the Luna story and there was no chapter two.** She had completed
episode 1 in full — both choices, both practice gates — and it simply stopped.
`39ea709` adds "Luna and the Light That Told Lies", built on the last image of
chapter one. A Luna Moth circles the porch lantern; a Luna Moth has no mouth and
lives about a week, so every hour spent circling a lamp is an hour not spent
finding another moth. She reads Bachan's field guide (reading gate), counts lamps
door to door (maths gate), the street goes dark, and two moths find each other.
Both facts come from `SPECIES_CATALOG`, and a test pins the story to the catalog
so they cannot drift — including one forbidding the moth ever being shown
feeding, which is the obvious warm ending and would contradict what the chapter
just taught.

It also fixes the handoff: finishing a chapter used to return to the garden with
no hint the story had grown.

**A fifth of her sessions ended with zero answers.** They were never *ended*, and
they clustered on the two mic-based skills — which is how the blocked-mic dead
end in §3 was found. Not proven to be the cause on her tablet, but the dead end
is real and the data fits.

---

## 6. Verification state

| Verified how | What |
|---|---|
| Tests (1,148 passing) + typecheck + production build | all logic, all catalogs, all gating, both bird tiers, the seasonal arrival, the seed guards |
| Against the live database | the strand split, the species and habitat rows, the restore and its per-skill attribution |
| In a running browser | the wrong-bird fix (five questions, five distinct clips, each mapped back to its bird), the spectrogram aspect ratio, the life-list celebration, Cliffside Point expanded, Esme's tier, Luna chapter two, the winter crew present in October and absent in August |
| Contact sheets, by eye | all 16 new candidates per role |
| **On the actual device** | **sound plays and clips are loud enough; the Nature Walk chooser has been used; the life list is in genuine use — Cecily has logged 8 species with repeats** |
| **Never touched by a finger** | **Listen and Match end-to-end, tapping a bird in the garden, Cliffside Point at portrait width, Esme's tier, Luna chapter two, the winter crew** |

That last row is the risk. Five sessions of work have now been built without a
device pass, and this session alone produced four bugs that only the running app
revealed.

---

## 7. What is next

1. **The device pass.** Overdue by five sessions. The specific unknowns are in the
   last row above.
2. **Audio for crew 3.** `birds:audio-harvest --bird dark_eyed_junco` etc., then
   the audition page. Listen/Match units for the winter crew do not exist yet and
   the existing units already hide until their crew has clips, so nothing breaks
   in the meantime.
3. **High Meadow** is still crowded — 14 stops on a 100-unit pitch with 92-wide
   labels, spanning wider than the portrait window. One lighter since Leftover
   Rocks moved.
4. **Habitat interiors letterbox badly at portrait width.** Pre-existing (the
   pond does it identically), but it wastes half a tablet screen.
5. **The five accessibility-settings tests** have failed on clean main since
   before any of this.

**Open question for the user:** the full tier can offer crew-2 bird names as
distractors in crew-1 units, before she has met them. It arguably makes those
questions easier rather than harder. Left alone deliberately, because changing it
changes Cecily's experience.

---

## 8. Operational notes

- **Tests hang if any `next dev` is running** — including one from a *different
  project*. This bit three times in this session; the culprit was
  `neurogenetics-notes-app`. Kill it, then `npx vitest run tests/
  --no-file-parallelism`.
- **Adding a species or habitat still needs a seed run**, because `journal_entry`
  has a foreign key to `species`. That is now safe by default (§2), but it is
  still the reason a seed is needed at all.
- **A new habitat needs a `HABITAT_QUESTS` entry** or it can never be built — the
  build modal renders `null` without one, silently.
- **`SpeciesIllustration` must return null for unknown codes.** Callers do
  `?? <emoji>`, and an empty element is truthy.
- **Do not change `storagePathFor`** in either upload script — it would orphan
  existing rows and duplicate the catalog on the next run.
- Two children's numbers, as of writing: Cecily 2,061 lifetime correct, 4 bird
  units, 8 birds on her life list. Esme 406, 0 bird units, 1.
