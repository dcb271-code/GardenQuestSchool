# Handoff — 2026-08-19

Continues [`2026-08-12-handoff.md`](./2026-08-12-handoff.md), which
covered `5de2fcf`…`326f12d`. This one covers `30b460a`…`33fc066` —
14 commits over a week. All pushed; Vercel auto-deploys.

The week in one sentence: **the house opened, the animals got fed,
and the cavern learned to teach** — three of the biggest standing
promises to Cecily, all delivered against her letters, with the owner
art-directing the rooms by screenshot.

---

## 1. What shipped

### The house — phases 1 and 2
`8cd976c` `d748d0c` `c5d0e68` `626744f` `b228319` `e08d734` `0b1ee95`

Tapping the house on the garden map opens `/garden/house` — the
first interior in the app with **no gate**, because nobody earns
their own front door. Drawn from photos of the real house, with the
owner's corrections recorded in the spec (exterior unchanged; a
cushioned bench in front of the stairs, not the listing's staged
table).

**The entry hall**: the oak staircase climbing out of frame left
(mirrored to how you really walk in, after owner feedback), the
hallway ahead to the kitchen, the reading room as a firelit doorway
off the right, a coat hook per child with the signed-in child's coat
gently swinging. **The reading room**: the tiger-oak mantel with two
display slots she fills herself — a stone from her case, a bird from
her life list, ownership checked server-side — Luna asleep on the
hearth, and finished Luna chapters as re-readable storybooks that
print the choices SHE made as what happened. **The kitchen**: her
real one — sage shakers, starburst backsplash, French-door fridge —
opening the same `KitchenModal` recipe system as Bachan's table. One
kitchen system, two doors, nothing forked.

Unbuilt on purpose: upstairs (her room — phase 3, waiting on the art
store and her letter about what goes in it). Signs say "not built
yet" honestly; in your own house a padlock is a wrong note.

**Art direction happened by owner screenshot review** — four rounds
on the foyer (flip the composition, remove the double doors, ground
the bench, calm the rug) and one full kitchen redraw ("that skinny
refrigerator?!"). The recurring caught bug class is FLOATING: the
bench, the fern, the study table all initially hovered. The rule
that must join every proof-render pass: *does everything stand on
something, with its shadow at its feet?*

### Treats — shop phase three
`e08d734`

"Way can't I feed the browing animals?" — now she can. Four treat
kinds (5–15p) matched to diet: seed cake, garden greens, bug tin,
nectar drop. Bought into a pantry (consumable, never placed), given
by tapping a resident on the garden map: the name bubble grows a
"give a treat" button when she carries the right kind and the animal
is unfed today. The animal tells one true thing, rotating funFact and
description. Caps inherit everything: one per animal per day on HER
day, server-side, refusals in words, treats only ever cost coins.
Verified end-to-end: bought a seed cake as Cecily, fed the cardinal,
got Kentucky's state bird story back.

### The gem curriculum — the study table
`d3a6c30` `4ffc667` `57a5811` `33fc066`

The unbuilt half of her Crystal Cavern commission: the LEARNING.
A study table drawn into the cavern (standing on the floor — it
floated first, of course) opens `/gems`: six one-pass units in the
bird module's shape, all exercises **generated from GEM_CATALOG**
with a seeded rng so nothing can drift from the catalog.

The seam crew (open now): rock-mineral-or-once-alive via the
state-symbols-backwards story; the scratch test, ending with a real
assignment in the real garden; how stones get made; and the
fluorspar nobody wanted. The case crew (opens when the seam is
learned): ruby and sapphire as one mineral, and diamond as
billion-year-old Kentucky-coal carbon.

**First pass pays the unit's own stone** into the cavern's
keep-or-sell flow, once ever — a third coin source, bounded by
content, recorded in the shop spec's ledger. Case units pay seam
stones (a lesson must never hand over a Great-Work-grade gem — a
test enforces it); the rewards rhyme instead: the diamond unit pays
coal, which is the joke. Tests pin honesty hard: Mohs contests
checked against real numbers and requiring a ≥1.5 gap, story facts
that must not name their own stone, shape questions where exactly
one offered stone grows the shape (fluorite and galena both grow
cubes — a question with two right answers is a lie).

Practice writes `source: 'gems'` null-item attempt rows — third
subject on that pattern.

### The flower grows with her, and other letter fixes
`a93d1fa`

- **Map stops now support `levelUpgrade`**: Part & Whole (🌸, her
  "flower in the meadow") serves number bonds within 20 at Level 3+
  instead of the Level-1 within_10 forever. Session start, progress
  counting and sublabel all resolve consistently.
- **Meeting a cavern animal no longer costs a dig** — the roll
  happens before recordDig. Bounded: four creatures ever, per child.
- **The arrivals "bug" was diagnosed as emptiness**: Cecily has all
  25 attractable species. Three more animals exist behind things she
  hasn't done (owl box build ×2, log-pile researcher badge ×1). She
  was told exactly that by letter. The world still doesn't say
  "full" by itself — see backlog.

### The letterbox bit its owner
`7734cb8`

One reply slot per letter: replying to an already-answered letter
silently REPLACES the old reply and — worse — inherited its readAt,
so the new reply was born invisible. Found by stepping in it while
announcing the study table. `replyTo` now clears readAt; the script
refuses to overwrite without `--force`. When announcing news, reply
to an unanswered letter.

---

## 2. Verification state

| Verified how | What |
|---|---|
| Tests (1,443+; the same 5 pre-existing settings failures) | all logic, catalogs, generators, gating, treats caps, storybooks, coat colors |
| `npm run smoke` — 23 routes as Cecily | every page renders |
| Real-browser click-throughs | house rooms (incl. a tap-swallowing overlay caught before ship), treat purchase + feeding the cardinal, gem unit walk, study-table tap-through |
| Proof-renders reviewed by the OWNER | foyer ×4, kitchen ×2 — this loop caught what my own eye did not |
| Still no device pass | now ~9 sessions of surfaces untouched by a finger |

Operational notes this week: `next dev` from OTHER projects still
hangs the vitest suite (bit twice; the second time the other server
belonged to an active session and the full-suite run was skipped in
favor of targeted tests + build + smoke — noted in the commit).
Background `next start` processes from one Bash call don't survive
to the next; start server and smoke in ONE shell invocation.

---

## 3. Data findings worth keeping

**Cecily's runway** (measured 08-18): 72 of 103 skills mastered, 82
touched, **21 untouched** — nearly all the hard L4/L5 math
(fractions arithmetic, decimals, multi-digit multiplication, long
division, area/perimeter, volume) plus three reading skills. She is
NOT out of curriculum; she's outrunning the *world* (all species
arrived, all creatures met) while the paths steer her to comfort:
27 of her last 60 sessions were times-table facts, 6 were
counting-to-50. The steering, not the content, is the gap.

**She has never opened Luna's adventure** — save is null. Two full
chapters unread. Esme finished chapter 1. Probably the treat-or-story
choice always resolves to treat. Free content runway sitting there.

---

## 4. The promise ledger (kept vs. open)

KEPT this week: the house (×3 asks), feeding the animals, the gem
curriculum ("learn how gems form…"), more things to buy (treats),
the kitchen.

STILL OPEN, in her words: **the art store** (promised to both girls
repeatedly, including "the exploit diamond is paying for it"), **her
own room upstairs**, and now **"way can't I open the refrigerator
door or the curb door"** (2026-08-19 — she found the kitchen and
wants the doors to open; classic and cheap whimsy).

Unanswered letters as of this writing: Cecily 2 (the fridge-door one
plus a ❤️), Esme 9, Otto 3, Friends 2. Esme's are ~80% art store.

---

## 5. Prioritized backlog

1. **The art store.** The oldest unkept promise, owed to both girls
   in writing. Needs one design decision from the owner first: shop
   for art things only, or an actual make-art surface (easel/canvas)?
   Esme's nine letters wait on this.
2. **Crew-3 bird audio — HARD DEADLINE mid-October.** Juncos reach
   Louisville then; Listen/Match units stay hidden until clips
   exist. Pipeline is proven (`birds:audio-harvest` → clips →
   audition on :4517 → upload), needs a human ear session.
3. **Kitchen doors that open** (her newest letter) + reply to her
   two waiting letters. Small, high-delight, keeps the letterbox
   covenant.
4. **The device pass.** ~9 sessions of untouched surfaces. The owner
   review loop on screenshots has been catching a lot — but the
   tablet is the judge, and treats/gems/house have never felt a
   finger.
5. **Steering to the hard 21.** Extend `levelUpgrade` to the other
   outgrown stops (counting-to-50 first) and make Hodge/compass
   point at untouched L4/L5 skills instead of comfort loops.
6. **Make the world say "full."** When no species can arrive, the
   garden should say so and name what would change it (owl box,
   log-pile badge). Emptiness that looks like a bug generates
   letters.
7. **Esme and Otto reply pass** — and one small thing aimed at Otto
   (Cecily asked; his garden is 2 sessions old).
8. **Luna adventure discoverability** — she's never seen chapter 1;
   cheap fix before anyone writes chapter 3.
9. **Upstairs / her room** (house phase 3) — deliberately after the
   art store; needs her letter about what goes in it.
10. **The five settings tests** failing on clean main since before
    the birds.

---

## 6. Operational quick-reference

- Letters: `npm run letters -- --unanswered` every session. One
  reply slot per letter; `--force` to overwrite, and don't.
- Smoke: build, then `npx next start` and `node
  scripts/smoke-routes.mjs --learner <id>` in the SAME shell call.
- Kill other projects' `next dev` before full vitest — unless it
  belongs to a live session; then run targeted tests and say so.
- Proof-render checklist gained a line: everything stands on
  something, shadow at its feet.
- Migrations are still manual; nothing this week needed one (all new
  state rode existing jsonb conventions: `garden.house`,
  `garden.shop.pantry`, `garden.gem_units`, `cavern.lessonPaid`).
